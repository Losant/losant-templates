resourceType: ExperienceEndpoint
resources:
  - access: public
    description: The registration endpoint that devices can POST to when registering
      themselves.
    enabled: true
    endpointTags: {}
    id: ~losant-experienceEndpoint-postApiRegisterDevice-0~
    method: post
    route: /api/register-device
    version: develop
version: 1
