application.register("cpf-page-devices", class extends Controller {

  connect() {

    if (document.documentElement.hasAttribute("data-turbo-preview")) {
      return;
    }

    console.log('cpf-page-devices');

  };

});